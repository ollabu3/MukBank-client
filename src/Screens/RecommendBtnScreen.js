import React, { useState, useEffect } from 'react';
import { View, Text } from 'react-native';

export default function RecommendBtnScreen() {
  const [locationX, setLocationX] = useState(null);

  return (
    <View>
      <Text>RecommendBtn</Text>
    </View>
  );
}
